# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# ModelMetadata CRD for P2P worker coordination.
# Copied from examples/crds.yaml — owned here so CI does not depend on
# the examples directory layout.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: modelmetadatas.modelexpress.nvidia.com
  labels:
    app.kubernetes.io/name: modelexpress
    app.kubernetes.io/component: crd
spec:
  group: modelexpress.nvidia.com
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required:
                - modelName
              properties:
                modelName:
                  type: string
                  description: Full model name (e.g., meta-llama/Llama-3.1-405B-Instruct)
            status:
              type: object
              properties:
                worker:
                  type: object
                  description: Single worker NIXL metadata and readiness state (one CR per worker)
                  required:
                    - workerRank
                  properties:
                    workerRank:
                      type: integer
                      description: Worker rank (0-indexed)
                    backendType:
                      type: string
                      description: Backend type discriminator (nixl, transfer_engine, none)
                      enum:
                        - nixl
                        - transfer_engine
                        - none
                    nixlMetadata:
                      type: string
                      description: Base64-encoded NIXL agent metadata blob
                    transferEngineSessionId:
                      type: string
                      description: Mooncake TransferEngine session ID (ip:port)
                    tensorCount:
                      type: integer
                      description: Number of tensors registered by this worker
                    tensorConfigMap:
                      type: string
                      description: Name of ConfigMap containing tensor descriptors
                    tensorCatalogConfigMap:
                      type: string
                      description: Name of ConfigMap containing lightweight tensor catalog entries
                    tensorCatalogGeneration:
                      type: integer
                      format: int64
                      description: Monotonic generation of the lightweight tensor catalog
                    tensorCatalogCount:
                      type: integer
                      description: Number of entries in the lightweight tensor catalog
                    metadataEndpoint:
                      type: string
                      description: P2P NIXL listen thread endpoint (host:port)
                    agentName:
                      type: string
                      description: P2P NIXL agent name for remote identification
                    workerGrpcEndpoint:
                      type: string
                      description: P2P worker gRPC endpoint for tensor manifest (host:port)
                    labels:
                      type: object
                      description: |
                        Free-form key/value labels describing this worker's external
                        identity (e.g. pod, namespace, node, rack). Used by the planner's
                        external-data informers to join MX peers against signals tagged by
                        deployer-meaningful keys.
                      additionalProperties:
                        type: string
                    status:
                      type: string
                      description: Worker lifecycle status
                      enum:
                        - Unknown
                        - Initializing
                        - Ready
                        - Stale
                      default: Unknown
                    updatedAt:
                      type: string
                      format: date-time
                      description: Timestamp of last status update (RFC3339)
                conditions:
                  type: array
                  description: Conditions for ModelMetadata lifecycle
                  items:
                    type: object
                    required:
                      - type
                      - status
                    properties:
                      type:
                        type: string
                        description: Condition type
                        enum:
                          - Ready
                      status:
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      reason:
                        type: string
                        description: Machine-readable reason for condition
                      message:
                        type: string
                        description: Human-readable message
                      lastTransitionTime:
                        type: string
                        format: date-time
                observedGeneration:
                  type: integer
                  format: int64
                  description: Generation observed by the controller
                publishedAt:
                  type: string
                  format: date-time
                  description: Timestamp when first worker published
      additionalPrinterColumns:
        - name: Model
          type: string
          description: Model name
          jsonPath: .spec.modelName
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
  scope: Namespaced
  names:
    plural: modelmetadatas
    singular: modelmetadata
    kind: ModelMetadata
    shortNames:
      - mxmeta
      - mm
