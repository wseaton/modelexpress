# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "modelexpress.fullname" . }}
  labels:
    {{- include "modelexpress.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "modelexpress.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "modelexpress.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "modelexpress.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- if .Values.securityContext }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          {{- end }}
          {{- if .Values.image }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
          {{- end }}
          ports:
            - name: grpc
              containerPort: {{ .Values.service.port | default 8001 }}
              protocol: TCP
          {{- if or .Values.env .Values.extraEnv .Values.p2pAuth }}
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.p2pAuth.enabled }}
            - name: MODEL_EXPRESS_SECURITY_MODE
              value: {{ .Values.p2pAuth.mode | quote }}
            - name: MODEL_EXPRESS_SECURITY_TOKEN_AUDIENCES
              value: {{ .Values.p2pAuth.audiences | quote }}
            {{- if .Values.p2pAuth.deviceResources }}
            - name: MODEL_EXPRESS_SECURITY_DEVICE_RESOURCES
              value: {{ .Values.p2pAuth.deviceResources | quote }}
            {{- end }}
            {{- if .Values.p2pAuth.deviceClasses }}
            - name: MODEL_EXPRESS_SECURITY_DEVICE_CLASSES
              value: {{ .Values.p2pAuth.deviceClasses | quote }}
            {{- end }}
            {{- if .Values.p2pAuth.podLabelSelector }}
            - name: MODEL_EXPRESS_SECURITY_POD_LABEL_SELECTOR
              value: {{ .Values.p2pAuth.podLabelSelector | quote }}
            {{- end }}
            - name: MODEL_EXPRESS_SECURITY_CACHE_TTL_SECS
              value: {{ .Values.p2pAuth.cacheTtlSeconds | quote }}
            {{- else }}
            # Auth disabled: force off so the server's in-cluster permissive default
            # does not silently take effect.
            - name: MODEL_EXPRESS_SECURITY_MODE
              value: "off"
            {{- end }}
            {{- if .Values.extraEnv }}
            {{- toYaml .Values.extraEnv | nindent 12 }}
            {{- end }}
          {{- end }}
          {{- if .Values.command }}
          command:
            {{- toYaml .Values.command | nindent 12 }}
          {{- end }}
          {{- if .Values.args }}
          args:
            {{- toYaml .Values.args | nindent 12 }}
          {{- end }}
          {{- if and .Values.livenessProbe .Values.livenessProbe.enabled }}
          livenessProbe:
            {{- if .Values.livenessProbe.tcpSocket }}
            tcpSocket:
              {{- toYaml .Values.livenessProbe.tcpSocket | nindent 14 }}
            {{- end }}
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds | default 5 }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds | default 5 }}
            failureThreshold: {{ .Values.livenessProbe.failureThreshold | default 3 }}
          {{- end }}
          {{- if and .Values.readinessProbe .Values.readinessProbe.enabled }}
          readinessProbe:
            {{- if .Values.readinessProbe.tcpSocket }}
            tcpSocket:
              {{- toYaml .Values.readinessProbe.tcpSocket | nindent 14 }}
            {{- end }}
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds | default 5 }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds | default 5 }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold | default 3 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if or (and .Values.persistence .Values.persistence.enabled) .Values.extraVolumeMounts }}
          volumeMounts:
            {{- if and .Values.persistence .Values.persistence.enabled }}
            - name: model-cache
              mountPath: {{ .Values.persistence.mountPath | default "/root" }}
            {{- end }}
            {{- if .Values.extraVolumeMounts }}
            {{- toYaml .Values.extraVolumeMounts | nindent 12 }}
            {{- end }}
          {{- end }}
      {{- if or (and .Values.persistence .Values.persistence.enabled) .Values.extraVolumes }}
      volumes:
        {{- if and .Values.persistence .Values.persistence.enabled }}
        - name: model-cache
          persistentVolumeClaim:
            claimName: {{ include "modelexpress.fullname" . }}-pvc
        {{- end }}
        {{- if .Values.extraVolumes }}
        {{- toYaml .Values.extraVolumes | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
